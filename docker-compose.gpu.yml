version: '3.8'

# GPU override - use with: docker-compose -f docker-compose.yml -f docker-compose.gpu.yml up
services:
  worker:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
